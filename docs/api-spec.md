## API Spec – Audio Dedup Platform

This doc tracks the primary HTTP endpoints exposed by the NestJS backend.

Planned endpoints:

- `POST /api/audio/upload`
- `GET /api/audio`
- `GET /api/audio/:id`
- `DELETE /api/audio/:id`
- `GET /api/audio/:id/stream`
- `GET /api/duplicates`
- `GET /api/duplicates/stats`
- `GET /api/health`

Endpoint request/response schemas will be added as the backend is implemented.

