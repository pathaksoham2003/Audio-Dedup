# App
NODE_ENV=development
PORT=3001

# PostgreSQL / Supabase
DATABASE_URL=postgresql://user:pass@postgres:5432/audio_dedup
SUPABASE_URL=https://temp.supabase.co
SUPABASE_SERVICE_KEY=temp.temp._9gJq5-3gB0g-temp
SUPABASE_STORAGE_BUCKET=audio-uploads

# Redis (for BullMQ)
REDIS_HOST=redis
REDIS_PORT=6379

# Python Audio Service
AUDIO_SERVICE_URL=http://audio-service:8000

# SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Dedup Thresholds
FINGERPRINT_SIMILARITY_THRESHOLD=0.85
EMBEDDING_SIMILARITY_THRESHOLD=0.92
SUSPICIOUS_SIMILARITY_THRESHOLD=0.75

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=20

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:3001/api

