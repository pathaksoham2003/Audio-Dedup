version: "3.9"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped

  audio-service:
    build:
      context: ./audio-service
      dockerfile: Dockerfile
    restart: unless-stopped

  postgres:
    image: postgres:16
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    restart: unless-stopped

